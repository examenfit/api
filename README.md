# ExamenFit API

## Installation
- Install PHP
- Install MySQL-database (MariaDB)
- Install pdftotext

# PDF2Text
sudo apt-get install poppler-utils

# KaTeX
npx katex

# Imagemagick
sudo apt install imagemagick
